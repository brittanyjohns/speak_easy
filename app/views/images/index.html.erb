<div class="mx-auto md:w-2/3 w-full">
  <div class="flex justify-between items-center mb-10">
    <h1 class="font-bold text-4xl">Image Library</h1>
    <%= link_to "New image", new_image_path, class: "rounded-lg py-3 px-5 outline block font-medium" %>
  </div>
  <%= render "images/search" %>

  <div class="my-6">
    <%= render "images", images: @images %>
  </div>
</div>
