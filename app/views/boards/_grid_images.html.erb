
<div class="grid grid-cols-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-0.5 p-1 w-full h-full">
  <% @images.each do |image| %>
    <div class='grid-cell'>
        <%= saved_image_link(image) %>
    </div>
    <% end %>
</div>
